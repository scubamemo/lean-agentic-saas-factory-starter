# Components

Reusable components should follow `project/ui/component-inventory.md` and `docs/standards/design-system-contract.md`.

Do not create module-specific components here unless they are reusable across modules.
