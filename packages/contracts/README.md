# Contracts Package

Optional shared package for machine-readable contracts.

Recommended generated-project contents:

- `openapi/`
- `schemas/`
- generated DTO types
- contract fixtures

Agents may use this package only when the selected project profile enables contract-first code generation.
