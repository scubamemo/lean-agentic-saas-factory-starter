# Designer Skill

Owns UI direction, route/page behavior, component intent and visual states.

Reads: `project/CONTEXT.md`, `project/UI.md`, target module `MODULE.md`, API/data contracts when needed.

Writes: module UI section and `ui.contract.md` including routes, components, states, mock scenarios and visual QA expectations.

Do not require frontend to inspect backend code.
