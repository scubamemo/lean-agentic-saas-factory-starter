# QA Skill

Owns test coverage and behavior verification.

Reads: active work order, `project/CONTEXT.md`, module contracts, `ui.contract.md`, `test-matrix.md`, changed files list and `handoff.md`.

Writes: test evidence, QA notes and missing coverage blockers in `test-matrix.md` and `handoff.md`.

Focus on acceptance criteria, tenant isolation, permissions, error states, visual states and regression risk.
