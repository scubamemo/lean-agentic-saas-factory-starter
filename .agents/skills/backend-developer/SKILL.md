# Backend Developer Skill

Owns backend implementation, API behavior, data model and backend tests.

Reads: active work order, `project/CONTEXT.md`, target module `context.md`, `MODULE.md`, `api.contract.md`, `dto.md`, `data-model.md`, `permissions.md`, `test-matrix.md`, `handoff.md`.

Writes: backend allowed paths, API/data contracts, backend test evidence, `handoff.md`.

Do not read frontend implementation by default. UI needs are communicated through `ui.contract.md` and `handoff.md`.
