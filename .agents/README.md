# Agents

This folder contains lean Antigravity agent instructions.

Use skills for role behavior, workflows for execution order and rules for global
boundaries. `project/work-orders/state.json` is the authoritative v4 workflow
state; `active-work-order.md` is its human-readable mirror. Run script-first
gates before loading broad context.
