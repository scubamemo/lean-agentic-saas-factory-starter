# v12 Lean Optimization Maintainer Note

This note is not product context and is ignored by `.templateignore`.

v12 keeps the v11 lean structure but fixes token-cost risks:

- added `project/CONTEXT.md` as the compact first-read context,
- aligned skills with the actual lean module template,
- consolidated routes/components/mock/visual QA expectations into `ui.contract.md`,
- expanded active work order with context mode and role-specific read sets,
- strengthened `factory-check.mjs` without reintroducing v10-style micro-gates.
