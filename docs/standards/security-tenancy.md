# Security and Tenancy Standard

Default tenancy is shared DB with `tenant_id`. Dedicated DB/app may be supported for premium customers through configuration and provisioning.

SuperAdmin can manage tenants and impersonate for support only when actions are audited and visibly marked.
